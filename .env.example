# ─── API Base URL ─────────────────────────────────────────────────────────────
# Leave commented out for local development — Vite proxies /api → localhost:3001
# Uncomment and set this only for production deployments on a separate domain
# VITE_API_BASE_URL=https://your-api-domain.com/api
